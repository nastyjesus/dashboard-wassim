{
  "name": "papa-tribu",
  "version": "0.1.0",
  "description": "Cloudflare Worker — backend communautaire de Papa Parfait (fil, posts, pouces, commentaires, signalements)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "wrangler": "^3.78.0"
  }
}
