{
  "name": "on-sort-poc",
  "version": "0.1.0",
  "description": "Cloudflare Worker — POC « On sort ? » : top 5 des sorties famille météo-aware autour de Rennes (week-end 1 du side hustle)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "wrangler": "^3.78.0"
  }
}
